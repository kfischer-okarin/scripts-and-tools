# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

See @docs/DESIGN.md for architecture and development methodology.

## Commands

```bash
./run-tests.sh                                # Run all tests
./build.sh                                    # Build binaries to bin/
go test -v ./internal/launcher -run TestName  # Run single test
```
