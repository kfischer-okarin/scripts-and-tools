#!/usr/bin/env bash

# Get the directory where this script is located
PROJECT_DIR="$(dirname $0)/../src/work-time"

eval $(mise env --cd $PROJECT_DIR)

exec bundle exec --gemfile $PROJECT_DIR/Gemfile $PROJECT_DIR/exe/worktime "$@"
